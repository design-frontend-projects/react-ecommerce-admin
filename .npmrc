; Node 26 ships an experimental localStorage global that warns on any access
; unless --localstorage-file is provided. Nothing server-side should use it,
; so disable the global for all pnpm-run scripts.
node-options=--no-experimental-webstorage
