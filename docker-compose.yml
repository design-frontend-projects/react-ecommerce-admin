name: ecommerce-restaurant
services:
  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
    deploy:
      replicas: 1
    networks:
      - app-network

  lb:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/lb.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
